Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator a plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-z)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{z+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator q plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{a+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-r)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{x+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator r plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-a)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{a+(x-q)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-s)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-c)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-w)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-b)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-x)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator q plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{q+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{r+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{s+(x-u)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator s plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{s+(x-a)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator a plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-t)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator p plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{p+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator w plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{t+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{p+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator r plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator t plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{t+(x-a)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator w plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator y plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{s+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-v)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator v plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{v+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator x plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{x+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup S\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator u plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{u+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator p plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{p+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator q plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{q+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-w)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{q+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-s)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{u+(x-w)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator s plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-v)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-p)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator a plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator t plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-t)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{x+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-r)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator v plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{v+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator a plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{a+(x-p)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator c plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{c+(x-t)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator w plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{w+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction with numerator s plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-s)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator a plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{a+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-q)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator a plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator v plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{v+(x-s)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-b)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{x+(x-y)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-y)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{b+(x-t)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-p)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{s+(x-w)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator v plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-c)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator w plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{w+(x-x)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the fraction with numerator y plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{y+(x-p)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-w)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator b plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in B union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup T\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator u plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{u+(x-y)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-w)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator t plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{t+(x-s)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{w+(x-a)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{q+(x-b)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator u plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{u+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{y+(x-v)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator q plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{q+(x-c)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator w plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-z)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator v plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{v+(x-q)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator q plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-r)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{b+(x-z)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the set of all x in the real numbers such that x is in T union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup T\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{a+(x-v)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator r plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{r+(x-p)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{c+(x-v)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator a plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{a+(x-r)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator p plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{p+(x-p)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{r+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{z+(x-x)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator c plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{c+(x-r)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction with numerator c plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{c+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator v plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{v+(x-u)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator q plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{q+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the absolute value of the fraction a plus b over c minus d, with scalable delimiters, raised to the power two
Output: \left|\left(\frac{a+b}{c-d}\right)\right|^{2}

Input: the fraction with numerator z plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-a)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{b+(x-a)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{y+(x-s)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in B union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup B\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator v plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{v+(x-t)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{z+(x-c)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator x plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{x+(x-r)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator x plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-w)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator t plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{t+(x-b)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{z+(x-q)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the fraction with numerator z plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{z+(x-t)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the fraction with numerator r plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{r+(x-w)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator r plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{r+(x-x)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-z)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator v plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{v+(x-x)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in A union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup A\land x>0\}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator b plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{b+(x-x)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator a plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{a+(x-s)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in A union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup B\land x>0\}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{r+(x-z)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the fraction with numerator p plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{p+(x-c)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator c plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{c+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{x+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the set of all x in the real numbers such that x is in S union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{r+(x-u)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{t+(x-c)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator u plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{u+(x-b)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator q plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{q+(x-q)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the fraction with numerator x plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{x+(x-b)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of C plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-u)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the fraction with numerator x plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{x+(x-t)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator a plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{a+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator b plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{b+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the fraction with numerator y plus the square of the quantity x minus a and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{y+(x-a)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator z plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{z+(x-p)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator u plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{u+(x-p)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in T union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup S\land x>0\}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the set of all x in the real numbers such that x is in T union B and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup B\land x>0\}

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the fraction with numerator y plus the square of the quantity x minus t and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{y+(x-t)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the fraction with numerator w plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{w+(x-b)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator w plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity y plus x squared
Output: \frac{w+(x-c)^{2}}{1+\left|y-z\right|}\left(y+x^2\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus p and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{s+(x-p)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: gamma of x plus zeta of s all over pi squared
Output: \frac{\Gamma(x)+\zeta(s)}{\pi^2}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity w plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(w+x^2\right)

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{s+(x-v)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the set of all x in the real numbers such that x is in B union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator p plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{p+(x-z)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the natural logarithm of one plus the square of g of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+g(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the product of the inverse of B plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the set of all x in the real numbers such that x is in S union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup S\land x>0\}

Input: the divergence of F plus the curl of G, all dotted with the gradient of f
Output: \left(\nabla\cdot \mathbf{F}+\nabla\times \mathbf{G}\right)\cdot \nabla f

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator b plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity t plus x squared
Output: \frac{b+(x-q)^{2}}{1+\left|y-z\right|}\left(t+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity b plus x squared
Output: \frac{c+(x-c)^{2}}{1+\left|y-z\right|}\left(b+x^2\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator z plus the square of the quantity x minus s and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{z+(x-s)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in A union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in A\cup T\land x>0\}

Input: the fraction with numerator t plus the square of the quantity x minus w and denominator one plus the absolute value of y minus z, all multiplied by the quantity s plus x squared
Output: \frac{t+(x-w)^{2}}{1+\left|y-z\right|}\left(s+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the set of all x in the real numbers such that x is in S union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup A\land x>0\}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the product of the inverse of B plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the fraction with numerator u plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity p plus x squared
Output: \frac{u+(x-r)^{2}}{1+\left|y-z\right|}\left(p+x^2\right)

Input: the greatest common divisor of a and b plus the least common multiple of a and b, all modulo n
Output: \left(\gcd(a,b)+\operatorname{lcm}(a,b)\right)\bmod n

Input: the fraction with numerator t plus the square of the quantity x minus q and denominator one plus the absolute value of y minus z, all multiplied by the quantity u plus x squared
Output: \frac{t+(x-q)^{2}}{1+\left|y-z\right|}\left(u+x^2\right)

Input: the set of all x in the real numbers such that x is in B union S and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in B\cup S\land x>0\}

Input: the product of the sum from k equals one to n of k squared over k plus one and the sum from k equals one to n of one over k
Output: \left(\sum_{k=1}^{n}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{n}\frac{1}{k}\right)

Input: the fraction with numerator z plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{z+(x-u)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the product of the inverse of B plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(B^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator s plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity z plus x squared
Output: \frac{s+(x-y)^{2}}{1+\left|y-z\right|}\left(z+x^2\right)

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the product of the inverse of A plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator y plus the square of the quantity x minus b and denominator one plus the absolute value of y minus z, all multiplied by the quantity v plus x squared
Output: \frac{y+(x-b)^{2}}{1+\left|y-z\right|}\left(v+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the inverse of C plus C and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+C\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator y plus the square of the quantity x minus c and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{y+(x-c)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the set of all x in the real numbers such that x is in T union A and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in T\cup A\land x>0\}

Input: the product of the inverse of C plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(C^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the set of all x in the real numbers such that x is in S union T and x is greater than zero
Output: \{ x\in\mathbb{R}\mid x\in S\cup T\land x>0\}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the fraction with numerator b plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity c plus x squared
Output: \frac{b+(x-u)^{2}}{1+\left|y-z\right|}\left(c+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus x and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{u+(x-x)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator p plus the square of the quantity x minus v and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{p+(x-v)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the product of the inverse of A plus A and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+A\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the fraction with numerator r plus the square of the quantity x minus r and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{r+(x-r)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction with numerator w plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity x plus x squared
Output: \frac{w+(x-y)^{2}}{1+\left|y-z\right|}\left(x+x^2\right)

Input: the fraction with numerator u plus the square of the quantity x minus z and denominator one plus the absolute value of y minus z, all multiplied by the quantity q plus x squared
Output: \frac{u+(x-z)^{2}}{1+\left|y-z\right|}\left(q+x^2\right)

Input: the fraction with numerator c plus the square of the quantity x minus u and denominator one plus the absolute value of y minus z, all multiplied by the quantity r plus x squared
Output: \frac{c+(x-u)^{2}}{1+\left|y-z\right|}\left(r+x^2\right)

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the probability of A given B plus the expected value of X squared, all divided by the variance of X plus sigma squared
Output: \frac{P(A\mid B)+E[X^2]}{\mathrm{Var}(X)+\sigma^2}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the binomial coefficient n choose k multiplied by two to the power n, all divided by n factorial plus one
Output: \frac{\binom{n}{k}2^n}{n!+1}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the fraction whose numerator is the integral from zero to one of x squared times sine of x and whose denominator is one plus the absolute value of y minus z
Output: \frac{\int_0^1 x^2 \sin(x)\,dx}{1+\left|y-z\right|}

Input: the product of the sum from k equals one to m of k squared over k plus one and the sum from k equals one to m of one over k
Output: \left(\sum_{k=1}^{m}\frac{k^2}{k+1}\right)\left(\sum_{k=1}^{m}\frac{1}{k}\right)

Input: the natural logarithm of one plus the square of h of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+h(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the fraction with numerator b plus the square of the quantity x minus y and denominator one plus the absolute value of y minus z, all multiplied by the quantity a plus x squared
Output: \frac{b+(x-y)^{2}}{1+\left|y-z\right|}\left(a+x^2\right)

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the product of the inverse of A plus B and the column vector with entries x and y, all inside a norm
Output: \left\|\left(A^{-1}+B\right)\begin{pmatrix} x \\ y \end{pmatrix}\right\|

Input: the density of a normal distribution with mean mu and variance sigma squared evaluated at x, multiplied by the indicator that x is greater than zero
Output: \mathcal{N}(\mu,\sigma^2)\cdot \mathbf{1}_{x>0}

Input: the modulus of the quantity one minus e to the i theta, divided by the real part of z plus one
Output: \frac{\left|1-e^{i\theta}\right|}{\Re(z)+1}

Input: the square root of the sum of the squares of x two minus x one and y two minus y one, all divided by one plus the absolute value of r
Output: \frac{\sqrt{(x_2-x_1)^2+(y_2-y_1)^2}}{1+\left|r\right|}

Input: the natural logarithm of one plus the square of f of x, multiplied by the limit as x approaches zero of sine of x over x
Output: \ln\left(1+f(x)^2\right)\left(\lim_{x\to 0}\frac{\sin(x)}{x}\right)
